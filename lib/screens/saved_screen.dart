import 'package:flutter/material.dart';

class GuardadoScreen extends StatelessWidget {

  const GuardadoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Guardado', style: TextStyle(fontSize: 24)),
    );
  }
}
