import 'package:flutter/material.dart';

class DescubreScreen extends StatelessWidget {

  const DescubreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Descubre', style: TextStyle(fontSize: 24)),
    );
  }
}
